Theosp Projects
===============

Quick start
-----------

Setup project.conf (see "project.conf structure" section)
node ./theosp-projects.js init
node ./theosp-projects.js register_submodules

project.conf structure
----------------------

project_path
    (string) The **full path** to the local project repository.

readable_name
    (String) The human-readable project name.

github_rep
    (string) The username/rep for the remote github repository of this project.
